# Slidecast

Digital-signage slidecast player — renders slides to TVs. Uses the baked Chromium runtime.

Waiveo extension — canonical repo. Published versions are git tags (vX.Y.Z); the Waiveo marketplace resolves this repo live.
